//package edu.wit.comp1050;
import org.apache.commons.configuration2.PropertiesConfiguration;
import org.apache.commons.configuration2.builder.fluent.Configurations;
import org.apache.commons.configuration2.ex.ConfigurationException;
import java.io.File;
import java.io.FileWriter;
/*
public class GameConfigs{
    public static final int _MinGuessRows = 8;
    public static final int _MaxGuessRow = 10;
    /**
     * makes configurations based on the values that were applied when program is launched again.
     * @param propertiesFileName file
     */
/*
    public GameConfigs(String propertiesFileName)
    {
     _configFile = new File(getClass().getResource(propertiesFileName).getFile());
     _config = configurations.properties(_configFile);
        }
    }
   private File _configFile;
    private PropertiesConfiguration _config;
    private int _codeSize;
    private int _codePegRows;
    private boolean _blanksAllowedInCode;
    private boolean _dupsAllowedInCode;
} */
